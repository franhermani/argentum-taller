#include "input_handler.h"

InputHandler::InputHandler() {}

// TODO: de algun lado llamar a este metodo y hacer command->execute(player)
Command* InputHandler::handleInput() {
    // TODO: para cada mensaje recibido del client devuelvo el command asociado
//    if (...) return moveComand(direction);

//    if (nada)
    return NULL;
}
